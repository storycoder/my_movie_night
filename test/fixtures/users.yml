alice:
  name: alice
  email: alice@example.com
  encrypted_password: <%= Devise.bcrypt(User, 'password') %>

bob:
  name: bob
  email: bob@example.com
  encrypted_password: <%= Devise.bcrypt(User, 'password') %>
