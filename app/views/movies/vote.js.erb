$('.movie-list tr[data-movie-id="<%= @movie.id %>"] .vote-count').html("<%= @movie.votes.count %>");

('.vote-history tbody').prepend("<%= escape_javascript(render partial: 'events/vote-history_row', locals: { vote: @vote }) %>");